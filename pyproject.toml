[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "buried-signals-benchmarks"
version = "0.1.0"
description = "Reproducible benchmarks for document parsing, browser automation, scraping, and search tools."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
bs-bench = "benchmarkers.cli:main"

[tool.setuptools]
packages = ["benchmarkers"]
